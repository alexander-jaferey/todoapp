# todoapp

basic to-do list application supporting multiple lists and deletion of tasks.

### database setup using flask-migrate
cd into project directory and run `flask db init` to initialize the migration repository. generate an initial migration with `flask db migrate -m "initial migration"` and apply database changes with `flask db upgrade`.

## todos
make it prettier